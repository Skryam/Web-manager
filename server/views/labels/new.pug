extends ../layouts/application.pug

block content
  h2.display-4.fw-bold.mt-4= t('views.labels.new.create')

  +formFor()(action= route('labels') method='post')
    +input(label, 'name', errors, t('views.labels.new.name'))(type="text")
    +submit(label)(value=t('views.labels.new.createButton'))